# Inputs: two (or N) origins, mode(s), time budget policy (e.g. “70% of pairwise time”)
# Output: overlap polygon(s) + derived search centers (centroid/representative point)
# + search radius (and/or a list of seed points)
